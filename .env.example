# MongoDB Connection String
# Replace with your actual MongoDB Atlas connection string
MONGO_STRING=mongodb+srv://username:password@cluster.mongodb.net/prestamos?retryWrites=true&w=majority

# Alternative environment variable name (Netlify commonly uses this)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/prestamos?retryWrites=true&w=majority

# Port for local development (not used in Netlify)
PORT=4000
